<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XBasicSeatingChart.PrePlacePageRight"
             x:Name="pppr"
             xmlns:local="clr-namespace:XBasicSeatingChart"
             BindingContext="{StaticResource commonVM}" local:PrePlacePageRight.RowCount="{Binding Rows}" local:PrePlacePageRight.ColumnCount="{Binding Columns}">
  <ContentView.Content>
        <Grid Background="{Binding RightColor}" Padding="10" x:Name="prePlacePageRightGrid">

        </Grid>
    </ContentView.Content>
</ContentView>